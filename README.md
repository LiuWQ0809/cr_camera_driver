# CR Camera Driver

CR多摄像头驱动程序，用于AGX Orin平台的GMSL2摄像头系统。

## � 概述

### Node功能介绍

`cr_camera_node` 是一个高性能的多摄像头ROS2节点，主要功能包括：

- **🎥 多摄像头采集**: 支持5路GMSL2摄像头同时采集（30fps硬件帧率）
- **⚡ 硬件加速转换**: 使用NVIDIA VIC硬件加速器进行UYVY→RGB/BGR转换
- **🎯 智能帧率控制**: 基于frame_skip_ratio的动态帧率调节（30fps→10fps）
- **🧵 多线程架构**: 5个工作线程并发处理，线程安全设计
- **📊 实时统计监控**: 详细的V4L2采集、图像处理、topic发布统计
- **🔐 时间校准机制**: 防止负时间戳，确保时间同步准确性
- **�🚀 批处理优化**: 一次VIC转换生成多格式多尺寸输出

### 实现逻辑架构

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────────┐
│   V4L2 Camera   │    │  Image Publisher │    │    ROS2 Topics      │
│                 │    │                  │    │                     │
│ ┌─────────────┐ │    │ ┌──────────────┐ │    │ /cr/camera/bgr/...  │
│ │ video0-video4│ │───▶│ │ VIC Converter│ │───▶│ (5×960x768 BGR)     │
│ │ (30fps UYVY) │ │    │ │ (Hardware)   │ │    │ (9.9fps BGR)        │
│ └─────────────┘ │    │ └──────────────┘ │    │                     │
│                 │    │                  │    └─────────────────────┘
│ ┌─────────────┐ │    │ ┌──────────────┐ │
│ │Time Sync    │ │    │ │Thread Pool   │ │
│ │& Calibration│ │    │ │(5 workers)   │ │
│ └─────────────┘ │    │ └──────────────┘ │
└─────────────────┘    └──────────────────┘
```

**核心组件说明：**

1. **V4L2Camera**: 
   - 管理5个摄像头设备的V4L2接口
   - 实现时间校准机制（V4L2时间→系统世界时间）
   - 事件驱动的帧采集（select/poll机制）

2. **ImagePublisher**:
   - VIC硬件加速转换（UYVY→RGB/BGR）
   - 批处理架构：一次转换生成多个topic
   - 智能发布控制（时间校准完成后才发布）

3. **线程架构**:
   - 主线程：V4L2事件循环和select等待
   - 工作线程池：并发处理图像转换和发布
   - 线程安全：mutex保护共享数据结构

4. **时间同步机制**:
   - 统计前10帧V4L2时间与系统时间差值
   - 使用最大值+0.5ms作为校正偏移量
   - 防止发布负时间戳的ROS消息

## 🚀 快速开始

### 📋 前置要求检查

在启动相机之前，请确认以下要求：

```bash
# 1. 检查系统版本
cat /etc/nv_tegra_release  # 应显示L4T R36.4.3或更高

# 2. 检查ROS2环境
echo $ROS_DISTRO           # 应显示humble
ros2 --version             # 应显示Humble版本

# 3. 检查摄像头设备
ls -la /dev/video*         # 应显示video0-video4设备

# 4. 检查GMSL2驱动
lsmod | grep sgx           # 应显示sgx-yuv-gmsl2模块
```

### 🔧 相机初始化步骤

**首次使用必须执行初始化：**

```bash
# 1. 进入项目目录
cd /home/nvidia/camera_driver/cr_camera_driver

# 2. 设置执行权限
chmod +x scripts/*.sh

# 3. 执行相机初始化
sudo ./scripts/init_cameras.sh
```

初始化脚本会自动完成：
- ✅ 卸载现有GMSL2驱动模块
- ✅ 重新加载sgx-yuv-gmsl2驱动
- ✅ 配置5个摄像头设备参数
- ✅ 验证设备节点创建成功
- ✅ 设置合适的设备权限

**初始化成功标志：**
```bash
# 应该看到5个摄像头设备
$ ls -la /dev/video*
crw-rw---- 1 root video 81, 0 Jan  1 12:00 /dev/video0
crw-rw---- 1 root video 81, 1 Jan  1 12:00 /dev/video1
crw-rw---- 1 root video 81, 2 Jan  1 12:00 /dev/video2
crw-rw---- 1 root video 81, 3 Jan  1 12:00 /dev/video3
crw-rw---- 1 root video 81, 4 Jan  1 12:00 /dev/video4

# 验证驱动加载
$ lsmod | grep sgx
sgx_yuv_gmsl2          28672  5
```

### 🚀 启动相机系统

#### 方式1: 一键启动脚本（强烈推荐）

**最简单的使用方式：**
```bash
cd /home/nvidia/camera_driver/cr_camera_driver
./scripts/start_cameras.sh --high-performance
```

这个命令会：
- ✅ 自动设置Orin为最高性能模式
- ✅ 初始化GMSL2相机驱动 
- ✅ 编译项目代码（内部调用 `./scripts/build_driver.sh`；加 `--skip-build` 可跳过）
- ✅ 验证相机设备
- ✅ 启动相机节点（无图像显示窗口）

**其他启动选项：**
```bash
# 完整启动（包含所有步骤）
./scripts/start_cameras.sh

# 快速启动（跳过初始化，适合重复启动）
./scripts/start_cameras.sh --no-init --skip-build

# 仅配置不启动（用于调试）
./scripts/start_cameras.sh --config-only

# 查看详细日志
./scripts/start_cameras.sh --verbose

# 查看所有选项
./scripts/start_cameras.sh --help
```

#### 方式2: ROS2标准启动

**使用ROS2命令启动：**

```bash
# 1. 设置环境
source /opt/ros/humble/setup.bash
cd /home/nvidia/camera_driver/cr_camera_driver
source install/setup.bash

# 2. 启动节点（使用默认配置）
ros2 run cr_camera_driver cr_camera_node

# 3. 或者使用自定义配置文件
ros2 run cr_camera_driver cr_camera_node --ros-args -p config_file:=/path/to/your/cameras.yaml
```

#### 方式3: 手动步骤控制

**适合调试和开发的详细步骤：**

```bash
# 1. 设置高性能模式（可选但推荐）
sudo nvpmodel -m 0          # 设置为MAXN性能模式
sudo jetson_clocks           # 锁定最高频率
sudo jetson_clocks --fan 255 # 设置风扇最大转速

# 2. 初始化相机（首次运行或出错后）
sudo ./scripts/init_cameras.sh

# 3. 编译项目（代码修改后需要）
cd /home/nvidia/camera_driver/cr_camera_driver
./scripts/build_driver.sh --build-type Release

# 4. 设置环境变量
source /opt/ros/humble/setup.bash
source install/setup.bash

# 5. 启动相机节点
ros2 run cr_camera_driver cr_camera_node
```

### 📊 运行时监控

**启动成功后，节点会输出以下信息：**

```bash
# 1. 初始化日志
[INFO] [cr_camera_node]: CR Camera Driver started
[INFO] [cr_camera_node]: Loaded configuration with 5 cameras
[INFO] [cr_camera_node]: Camera 0 initialized successfully
...

# 2. 时间校准日志
[INFO] [cr_camera_node]: Camera 0 time calibration completed
[INFO] [cr_camera_node]: Camera 1 time calibration completed
...

# 3. 每5秒的性能统计表格
╔════════════════════════════════════════════════════════════════╗
║                         Camera Frame Rate Stats                ║
╠════════╦═════════════╦═════════════╦═════════════╦═════════════╣
║ Cam ID ║ V4L2 Capture║   Deal Rate ║   V4L2 Total║   Deal Total║
╠════════╬═════════════╬═════════════╬═════════════╬═════════════╣
║    0   ║      30.0   ║      10.0   ║        751   ║        158   ║
║    1   ║      30.1   ║      10.0   ║        752   ║        158   ║
║    2   ║      30.0   ║      10.0   ║        751   ║        158   ║
║    3   ║      30.1   ║      10.0   ║        752   ║        158   ║
║    4   ║      29.9   ║      10.0   ║        748   ║        158   ║
╚════════╩═════════════╩═════════════╩═════════════╩═════════════╝

# 4. Topic发布统计（包含时延信息）
╔═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║                                              Topic Publishing Statistics                                                    ║
╠════════╦══════════════════════════════════════════════╦═══════╦═══════════╦═══════╦═══════╦══════════════════════════════════╣
║Cam ID  ║                 Topic Name                   ║Format ║   Size    ║  Rate ║ Total ║        Latency (ms)              ║
║        ║                                              ║       ║           ║       ║       ║    Min   Max   Avg              ║
╠════════╬══════════════════════════════════════════════╬═══════╬═══════════╬═══════╬═══════╬══════════════════════════════════╣
║    0   ║ /cr/camera/bgr/front_right_960_768           ║ BGR   ║ 960x768   ║   9.9 ║   207 ║  21.6  22.5  21.9              ║
║    1   ║ /cr/camera/bgr/front_left_960_768            ║ BGR   ║ 960x768   ║   9.9 ║   206 ║  22.0  23.0  22.4              ║
║    2   ║ /cr/camera/bgr/left_960_768                  ║ BGR   ║ 960x768   ║   9.9 ║   205 ║  21.4  22.3  21.7              ║
║    3   ║ /cr/camera/bgr/right_960_768                 ║ BGR   ║ 960x768   ║   9.9 ║   205 ║  21.5  22.2  21.8              ║
║    4   ║ /cr/camera/bgr/rear_960_768                  ║ BGR   ║ 960x768   ║   9.9 ║   205 ║  21.2  22.0  21.5              ║
╚════════╩══════════════════════════════════════════════╩═══════╩═══════════╩═══════╩═══════╩══════════════════════════════════╝
```

**性能指标说明：**
- **V4L2 Capture**: 硬件采集帧率（应为30fps）
- **Deal Rate**: 图像处理帧率（应为10fps，即30fps÷frame_skip_ratio）
- **Topic Rate**: 实际发布帧率（应接近9.9fps）
- **Latency**: 发布延迟（从图像时间戳到发布时间的差值）
```bash
# 完整启动（包含所有步骤）
./scripts/start_cameras.sh

# 快速启动（跳过初始化，适合重复启动）
./scripts/start_cameras.sh --no-init --skip-build

# 仅配置不启动（用于调试）
./scripts/start_cameras.sh --config-only

# 查看详细日志
./scripts/start_cameras.sh --verbose

# 查看所有选项
./scripts/start_cameras.sh --help
```

## 功能特性

- ✨ **批处理优化**: 一次VIC转换支持多格式多尺寸输出
- 🏎️ **硬件加速**: VIC+GPU混合架构，49+ FPS转换性能
- 🎯 **多线程架构**: 5个工作线程，线程安全设计
- 📊 **实时统计**: 详细的性能监控和topic统计
- 🔧 **智能配置**: 简化的YAML配置，统一帧率控制
- 🚀 **高性能模式**: 自动优化Orin性能设置
- 📈 **接近理论极限**: 9.9 FPS发布率（理论10 FPS）

## Topic映射

当前配置只发布5个960×768的BGR topic，原先的`/cr/camera/rgb/front_*_full` 已移除以减轻链路带宽：

| 摄像头 | Topic名称 | 格式 | 尺寸 | 发布率 |
|--------|-----------|------|------|--------|
| Camera 0 | `/cr/camera/bgr/front_right_960_768` | BGR | 960x768 | ~9.9 FPS |
| Camera 1 | `/cr/camera/bgr/front_left_960_768` | BGR | 960x768 | ~9.9 FPS |
| Camera 2 | `/cr/camera/bgr/left_960_768` | BGR | 960x768 | ~9.9 FPS |
| Camera 3 | `/cr/camera/bgr/right_960_768` | BGR | 960x768 | ~9.9 FPS |
| Camera 4 | `/cr/camera/bgr/rear_960_768` | BGR | 960x768 | ~9.9 FPS |

**设备映射**：`/dev/video0` → 前右、`/dev/video1` → 前左、`/dev/video2` → 左侧、`/dev/video3` → 右侧、`/dev/video4` → 后视。

## 性能表现

**最新测试结果（高性能模式）：**
- 🔥 **VIC转换FPS**: 49+ FPS
- ⚡ **平均VIC时间**: 10.8ms (-6% vs 标准模式)
- 🚀 **平均GPU时间**: 1.6ms (-48% vs 标准模式)
- 📊 **Topic发布率**: 9.9 FPS (99%理论效率)
- 🎯 **VIC效率**: 54.9%
- 💪 **支持topic数**: 5个并发（全部为960×768 BGR）

## 系统要求

- Jetson AGX Orin
- JetPack 6.0 (L4T R36.4.3)
- ROS2 Humble
- OpenCV 4.x
- GMSL2摄像头驱动已安装

## 安装和编译

1. **定位项目目录**
```bash
cd /home/nvidia/camera_driver/cr_camera_driver
```

2. **安装依赖**
```bash
sudo apt update
sudo apt install ros-humble-cv-bridge ros-humble-image-transport
```

3. **编译项目**
```bash
cd /home/nvidia/camera_driver/cr_camera_driver
./scripts/build_driver.sh
source install/setup.bash
```

`build_driver.sh` 会在项目目录下生成 `build/`、`install/` 和 `log/`，支持 `--clean`、`--build-type`、`--cmake-arg`、`--colcon-arg` 等选项。

## 使用方法

### 🚀 方式1: 一键启动脚本（强烈推荐）

**最简单的启动方式：**
```bash
cd /home/nvidia/camera_driver/cr_camera_driver
./scripts/start_cameras.sh --high-performance
```

`start_cameras.sh` 会默认在启动前调用 `./scripts/build_driver.sh`（除非加 `--skip-build`），然后初始化驱动和节点。

**常用启动选项：**
```bash
# 标准启动（包含所有初始化步骤）
./scripts/start_cameras.sh

# 高性能模式启动（推荐，性能提升20%+）
./scripts/start_cameras.sh --high-performance

# 快速重启（跳过初始化和编译，适合调试）
./scripts/start_cameras.sh --no-init --skip-build

# 仅配置系统不启动节点（用于测试配置）
./scripts/start_cameras.sh --config-only

# 显示详细日志（用于排错）
./scripts/start_cameras.sh --verbose

# 查看所有可用选项
./scripts/start_cameras.sh --help
```

### 方式2: 手动步骤启动

如果需要更精细的控制，可以手动执行各步骤：

```bash
# 1. 设置高性能模式（可选但推荐）
sudo nvpmodel -m 0
sudo jetson_clocks
sudo jetson_clocks --fan 255

# 2. 初始化相机（仅首次需要）
./scripts/init_cameras.sh

# 3. 编译项目（或使用 `./scripts/build_driver.sh`）
cd /home/nvidia/camera_driver/cr_camera_driver
./scripts/build_driver.sh --build-type Release

# 4. 启动节点
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run cr_camera_driver cr_camera_node
```

### 方式3: 使用ROS2 Launch文件（已废弃）

```bash
# 注意：Launch文件可能不包含最新的批处理优化
ros2 launch cr_camera_driver cr_cameras.launch.py
```

## 配置说明

### YAML配置文件 (`config/cameras.yaml`)

```yaml
# 主要配置参数
global_settings:
  frame_skip_ratio: 3         # 帧跳过比例，控制发布频率
                              # 1=30fps, 2=15fps, 3=10fps
  use_vic_converter: true     # 启用VIC硬件加速（推荐）
  stats_interval: 5           # 统计信息打印间隔（秒）

# 每个相机的topic配置
cameras:
  - id: 0
    device: "/dev/video0"
    width: 1920
    height: 1536
    enabled: true
    resize_topics:
      - topic: "/cr/camera/bgr/front_right_960_768"
        width: 960
        height: 768
        format: "BGR"           # 支持RGB/BGR格式
        enabled: true
```

### 性能优化配置

**高性能模式效果对比：**
- **标准模式**: VIC 36 FPS, GPU 3.1ms, Topic 7.7 FPS
- **高性能模式**: VIC 49 FPS, GPU 1.6ms, Topic 9.9 FPS ⚡

**推荐设置：**
- `frame_skip_ratio: 3` - 平衡性能与数据量
- `use_vic_converter: true` - 必须启用硬件加速
- `num_buffers: 2` - 减少内存占用

### 重要说明

⚠️ **帧率控制机制**: 
- 相机硬件固定30fps采集
- `frame_skip_ratio`控制发布频率：30fps ÷ 3 = 10fps
- 实际达到9.9fps（99%效率）
- 批处理优化：一次VIC转换服务多个topic

## 验证和测试

### 🎯 快速验证

**1. 检查topic发布状态**
```bash
# 查看所有摄像头topic（应该看到5个topic）
ros2 topic list | grep /cr/camera

# 检查topic发布频率（应该在9.8-9.9 FPS）
ros2 topic hz /cr/camera/bgr/front_right_960_768

# 查看详细topic信息
ros2 topic info /cr/camera/bgr/front_right_960_768
```

**2. 性能监控**
```bash
# 查看系统资源使用（推荐安装jtop）
sudo pip3 install jetson-stats
jtop

# 查看CPU/GPU频率（高性能模式验证）
sudo jetson_clocks --show
```

**3. 查看实时统计**
节点启动后会每5秒显示详细统计信息：
```
╔══════════════════════════════════════════════════════════╗
║                    Topic Publishing Statistics           ║
╠════════════╦═══════════════════════╦══════╦══════╦══════╣
║  Camera ID ║      Topic Name       ║Format║ Size ║ Rate ║
╠════════════╬═══════════════════════╬══════╬══════╬══════╣
║      0     ║ bgr/front_right_960_768  ║ BGR  ║960x768║ 9.9║
║      1     ║ bgr/front_left_960_768   ║ BGR  ║960x768║ 9.9║
║      2     ║ bgr/left_960_768         ║ BGR  ║960x768║ 9.9║
║      3     ║ bgr/right_960_768        ║ BGR  ║960x768║ 9.9║
║      4     ║ bgr/rear_960_768         ║ BGR  ║960x768║ 9.9║
```

### 🔧 高级测试

**1. 录制和回放数据**
```bash
# 录制所有topic（数据量大，注意存储空间）
ros2 bag record -a

# 录制特定topic
ros2 bag record /cr/camera/bgr/front_right_960_768 /cr/camera/bgr/front_left_960_768

# 回放数据
ros2 bag play <bag_file>
```

**2. 图像查看**
```bash
# 使用rqt查看实时图像
rqt_image_view

# 命令行查看图像信息（不显示图像数据）
ros2 topic echo /cr/camera/bgr/front_right_960_768 --no-arr
```

**3. 性能基准测试**
```bash
# 运行VIC性能测试套件
cd test/
make run-performance

# 查看详细性能对比
make run-all
```

## 故障排除

### 🚨 常见问题

**1. 一键启动脚本失败**
```bash
# 检查脚本权限
ls -la scripts/start_cameras.sh

# 如果没有执行权限，添加权限
chmod +x scripts/start_cameras.sh

# 使用详细日志模式查看错误
./scripts/start_cameras.sh --verbose
```

**2. 摄像头设备未找到**
```bash
# 检查设备节点
ls -la /dev/video*

# 检查GMSL2驱动状态
lsmod | grep -E "(sgx|max92)"

# 重新初始化相机
sudo rmmod sgx-yuv-gmsl2 max9296 max9295
./scripts/init_cameras.sh
```

**3. 性能不达预期**
```bash
# 确认高性能模式
sudo nvpmodel -q  # 应显示MAXN
sudo jetson_clocks --show | head -5

# 检查VIC转换性能（应该>45 FPS）
# 在节点日志中查看 "VIC Conversion Performance Stats"

# 如果性能低，重新设置高性能模式
sudo nvpmodel -m 0
sudo jetson_clocks
```

**4. Topic发布率低于9 FPS**
```bash
# 检查frame_skip_ratio设置
grep -A2 "frame_skip_ratio" config/cameras.yaml

# 检查系统负载
jtop

# 验证相机硬件帧率
v4l2-ctl --device=/dev/video0 --get-parm
```

**5. ROS2环境问题**
```bash
# 检查ROS2环境
echo $ROS_DISTRO  # 应该显示humble

# 重新source环境
source /opt/ros/humble/setup.bash
source install/setup.bash

# 检查依赖包
ros2 pkg list | grep -E "(cv_bridge|image_transport)"
```

### 🛠️ 调试技巧

**启用详细日志：**
```bash
# 使用详细模式启动
./scripts/start_cameras.sh --verbose

# 或者设置ROS日志级别
export RCUTILS_LOGGING_SEVERITY=DEBUG
ros2 run cr_camera_driver cr_camera_node
```

**性能分析：**
```bash
# 监控实时性能
watch -n 1 "ros2 topic hz /cr/camera/bgr/front_right_960_768"

# 查看GPU/VIC使用率
sudo tegrastats
```

**单独测试相机：**
```bash
# 测试单个相机（不启动ROS节点）
gst-launch-1.0 v4l2src device=/dev/video0 ! fakesink

# 检查相机参数
v4l2-ctl --device=/dev/video0 --all
```

## 开发和定制

### 运行测试
项目包含了完整的VIC硬件加速测试套件，位于 `test/` 目录：

```bash
# 进入测试目录
cd test/

# 查看所有可用测试
make help

# 编译并运行所有测试
make run-all

# 只运行性能基准测试
make run-performance

# 编译单个测试
make test_hybrid_performance
```

测试包括：
- VIC硬件加速性能测试
- CUDA后端格式转换测试  
- PVA硬件能力测试
- 混合架构性能对比

详见 `test/README.md` 了解各测试的具体用途。

### 修改topic名称
编辑 `src/cr_camera_node.cpp` 中的 `camera_topics_` 映射：
```cpp
camera_topics_ = {
  {0, "/your_custom/topic/name"},
  // ...
};
```

### 添加新的摄像头类型
在 `scripts/init_cameras.sh` 中添加新的配置参数。

### 修改图像格式
在 `publishRealImage()` 函数中修改OpenCV颜色转换代码。

## 许可证

MIT License

## 支持

如有问题，请检查：
1. 硬件连接是否正确
2. 驱动是否正确安装
3. ROS2环境是否正确设置
4. 权限是否足够（可能需要sudo）

## 更新日志

### v2.0.0 (当前版本) - 批处理优化版本
- ✨ **批处理架构**: 一次VIC转换生成多格式多尺寸输出
- 🚀 **性能大幅提升**: VIC转换49+ FPS（+35%），GPU时间1.6ms（-48%）
- 🎯 **接近理论极限**: Topic发布率达到9.9 FPS（99%效率）
- 🔧 **简化配置**: 统一使用frame_skip_ratio控制帧率
- 📊 **详细统计**: 新增topic级别性能统计
- 🧵 **多线程优化**: 线程安全的VIC转换器，支持并发处理
- 🛠️ **一键启动**: 新增start_cameras.sh全自动启动脚本
- ⚡ **高性能模式**: 自动优化Orin频率设置

### v1.5.0 - VIC硬件加速版本
- 🏎️ **VIC硬件加速**: 启用VPI加速图像转换
- 🔄 **多线程架构**: 异步处理提升并发性能
- 📈 **性能监控**: 实时VIC转换统计

### v1.0.0 - 基础版本
- 📷 **多相机支持**: 5路GMSL2相机并发采集
- 🔄 **格式转换**: UYVY到RGB/BGR转换
- 📡 **ROS2集成**: 标准Image消息发布
